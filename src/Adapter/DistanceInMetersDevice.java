package Adapter;//Adapter
/**
 * The DistanceInMetersDevice interface defines a contract for objects that can measure distances in meters.
 */
public interface DistanceInMetersDevice {
    /**
     * Retrieves the distance measured by the device in meters.
     * @return the distance in meters.
     */
    double getDistance();
}

